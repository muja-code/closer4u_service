<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/style/reivew.css" />
    <title>가까운4이 세탁서비스</title>
  </head>

  <body>
    <header>
      <div class="logo">
        <img src="" />
        <h1>가까운4이 세탁서비스</h1>
      </div>
      <div class="buttons">
        <% try { %> <% if (userId && member===0) { %>
        <button><a href="">주문신청</a></button>
        <button><a href="/api/orders/business">주문신청내역</a></button>
        <button><a href="/api/orders/customers">주문접수내역</a></button>
        <button class="logout">로그아웃</button>
        <button><a href="/api/users">마이페이지</a></button>
        <% } else if (userId && member===1) { %>
        <button><a href="/api/orders/business">주문신청내역</a></button>
        <button><a href="/api/orders/companies">주문접수내역</a></button>
        <button><a href="/login_page">로그인</a></button>
        <button><a href="">회원가입</a></button>
        <button class="logout">로그아웃</button>
        <button><a href="/api/users">마이페이지</a></button>
        <% } %> <% } catch { %>
        <button><a href="/login_page">로그인</a></button>
        <button><a href="">회원가입</a></button>
        <% } %>
      </div>
      <hr />
    </header>
    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const logout = document.getElementsByClassName('logout');
        for (let i = 0; i < logout.length; i++) {
          logout[i].addEventListener('click', () => {
            axios({
              method: 'post',
              url: '/api/users/logout',
              data: {},
            }).then((response) => {
              window.location.href = '/';
            });
          });
        }
      });
    </script>
  </body>
</html>
